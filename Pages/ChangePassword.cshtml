@page
@model LabMaterials.Pages.ChangePasswordModel
@section Scripts
{
    <script type="text/javascript" language="javascript">
        const notyf = new Notyf({
            position: {
                x: 'center',
                y: 'top',
            },
            types: [
                {
                    type: 'error',
                    background: '#e3392c',
                    icon: {
                        className: 'fas fa-times',
                        tagName: 'span',
                        color: '#000000'
                    },
                    dismissible: true,
                    duration: 0,
                    ripple: true
                }
            ]
        });

        function showErrorNotification(message) {
            notyf.open({
                type: 'error',
                message: message
            });
        }

        @if (!string.IsNullOrEmpty(Model.ErrorMsg))
        {
            <text>
                    var errorMessageFromServer = @Html.Raw(Json.Serialize(Model.ErrorMsg));
                    showErrorNotification(errorMessageFromServer);
            </text>
        }
    </script>
}
@{


    <div class="py-4">

        <div class="d-flex justify-content-between w-100 flex-wrap">
            <div class="mb-3 mb-lg-0">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb bg-transparent p-2 mb-0">
                    <li class="breadcrumb-item d-flex align-items-center">
                        <img src="images/home.png" alt="Home" width="16" height="16" class="me-2" />
                        <a href="/Home" class="text-decoration-none">@Model.lblHome</a>
                    </li>
                    <li class="breadcrumb-item d-flex align-items-center">
                        <img src="images/password.png" alt="Home" width="16" height="16" class="me-2" />
                        @Model.lblUserProfile
                    </li>
                </ol>
            </nav>
                <h1 class="h4">@Model.lblUserProfile</h1>
               <!-- <p class="mb-0">Dozens of reusable components built to provide buttons, alerts, popovers, and more.</p> -->
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-8 mb-4 m-auto">
            <div class="card border-0 shadow components-section">
                <form method="post" class="card-body">
                    <input type="hidden" name="ToUpdateUserID" value="@Model.ToUpdateUserID" />
                    <input type="hidden" name="Username" value="@Model.Username" />
                    <div class="row mb-4">


                        <div class="col-lg-12 col-sm-12">
                             @* User type  *@
                            <div class="mb-4">
                                <label class="my-1 me-2" for="UserGroupID">@Model.lblUserGroupName</label>
                                <select class="form-select" id="UserGroupID" name="UserGroupID" disabled>
                                @{
                                    foreach (var item in Model.UserGroupsList)
                                    {
                                        var selectedAttr = item.UserGroupId == Model.UserGroupID ? "selected=\"selected\"" : "";
                                        @:<option value="@item.UserGroupId" @Html.Raw(selectedAttr)>@item.UserGroupName</option>
                                    }
                                }
                                </select>
                            </div>
                             <div class="mb-4">
                                <label for="JobNumber">@Model.lblJobNumber</label>

                                <input type="text" class="form-control" id="JobNumber" name="JobNumber"
                                    value="@Model.JobNumber" @(!Model.IsADUser ? "" : "disabled") />

                            </div>

                            @* <div class="mb-4">
                                <label for="Username">@Model.lblUserName</label>

                                <input type="text" class="form-control" id="Username" name="Username"
                                    value="@Model.Username"  @(!Model.IsADUser ? "" : "disabled")/>

                            </div> *@
                            <div class="mb-4">
                                <label for="UserFullName">@Model.lblFullName</label>

                                <input type="text" class="form-control" id="UserFullName" name="UserFullName"
                                    value="@Model.UserFullName"  @(!Model.IsADUser ? "" : "disabled")/>

                            </div>

                            @* Employee's Affiliation  *@
                            <div class="mb-4">
                                <label for="EmpAffiliation">@Model.lblEmpAffiliation</label>

                                <input type="text" class="form-control" id="EmpAffiliation" name="EmpAffiliation"
                                    value="@Model.EmpAffiliation"  @(!Model.IsADUser ? "" : "disabled") />

                            </div>

                            @* Transfer  *@
                            <div class="mb-4">
                                <label for="Transfer">@Model.lblTransfer</label>

                                <input type="text" class="form-control" id="Transfer" name="Transfer"
                                    value="@Model.Transfer"   @(!Model.IsADUser ? "" : "disabled")/>

                            </div>

                            <div class="mb-4">
                                <label for="Email">@Model.lblEmail</label>

                                <input type="email" class="form-control" id="Email" name="Email" value="@Model.Email"  @(!Model.IsADUser ? "" : "disabled")/>

                            </div>


                            <div class="mb-4">
                                <label for="Password">@Model.lblCurrentPassword</label>
                                
                                    <input type="password" class="form-control"  id="Password" name="Password" value="@Model.Password" />

                              
                            </div>
                            <div class="mb-4">
                                <label for="NewPassword">@Model.lblNewPassword</label>
                               
                    <input type="password" class="form-control"  id="NewPassword" name="NewPassword"
                        value="@Model.NewPassword" />
               
                            </div>
                            <div class="mb-4">
                
                                <label for="NewPasswordConfirm">@Model.lblConfirmNewPassword</label>
                                 <input type="password" class="form-control"  id="NewPasswordConfirm" name="NewPasswordConfirm"
                        value="@Model.NewPasswordConfirm" />
                               
                                
                            </div>
                            <div class="mb-4">


                                <div class="d-flex mb-2 mb-md-0" style="gap: 10px">


                                    <a href="Home"
                                        class="btn btn-outline-gray-600  align-items-center w-100">@Model.lblCancel</a>

                                    <input type="submit" value="@Model.lblUpdate"
                                        class="btn btn-sm btn-gray-800 align-items-center w-100"
                                        style="text-align:center;" />

                                </div>

                            </div>
                        </div>
                    </div>

                </form>
            </div>
        </div>
    </div>



        @* <div style="text-align:center; color:maroon">@Model.ErrorMsg</div> *@
       
    
}
